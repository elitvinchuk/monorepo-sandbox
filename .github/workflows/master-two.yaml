name: Check Application Two
on:
  push:
    paths:
      - 'apps/app2/**'
      - '!packages/**'

jobs:
  validate:
    name: Building application 2
    runs-on: ubuntu-latest
    steps:
      - name: Greet application two
        run: echo 'Hello from application two'

